import { Tables } from "@/database.types";

export const removeOutlierPredictions = (
  predictions: Tables<"predictions">[]
) => {
  // TODO: implement once we have a better understanding of the data
  return predictions;
};
